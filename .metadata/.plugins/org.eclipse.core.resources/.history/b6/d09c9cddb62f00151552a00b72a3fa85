//package etc;
//
//public class ch6andch7 {

/*
ch6. 

6-8. 클래스 변수: width, height
	 인스턴스 변수: kind, num
	 지역변수: card, k, n
		  
6-9. weapon, armor가 모든 marine에서 동일해야 하므로 static변수가 되어야 하고, 이 변수를 쓰는 메소드 역시 static이어야 함

6-10. b,c,d
6-11. b
6-12. c,d
6-13. b
6-14. c,e
6-15. a
6-16. a,d,e
6-17. b
6-18. A: 클래스 변수는 인스턴스 변수를 사용할 수 없음	
	  B: 클래스 메소드는 인스턴스 변수를 사용할 수 없음
	  C: 클래스 메소드는 인스턴스 메소드 사용할 수 없음
6-19: ABC123

ch7.

7-3. 오버라이딩은 조상클래스로 상속받은 메소드 내용을 변경하는 것으로 코드 재사용성을 향상시킬 수 있음
7-4. e
7-5. Tv클래스에서 자동으로 조상클래스인 Product()를 호출하는데
현재 Product클래스에 int형 매개변수를 받는 생성자가 있어 자동으로 기본 생성자인 Product()를 호출 할 수 없어서 에러 발생
Product()를 추가해서 

7-6. 자손클래스의 인스턴스를 생성하는 것은 조상과 자손의 멤버가 모두 합쳐진 인스턴스가 생성되는 것으로 자손 클래스 인스턴스가
조상클래스 멤버를 사용할 수 있으므로 조상클래스 멤버의 생성과 초기화 작업이 선행되어야 하기때문임
7-7. Parent(int x) -> Parent() -> Child(int x) -> Child(), 결과는 200
7-8. a
7-9. b
7-12. c,e
7-13. Math클래스가 상수와 static메소드로만 구성되어 있기때문에 instance를 생성할 필요 없어
 외부로부터 불필요한 접근을 막기 위해서 접근제어자 private이 적절함
7-15. b
7-16.e

	*/
	
	
//}